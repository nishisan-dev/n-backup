#!/bin/bash
set -e

# Parar e desabilitar o serviço antes da remoção
if systemctl is-active --quiet nbackup-server.service; then
    systemctl stop nbackup-server.service || true
fi
systemctl disable nbackup-server.service || true
systemctl daemon-reload || true
